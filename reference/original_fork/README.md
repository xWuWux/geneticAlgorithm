# Original copies (unmodified, kept for provenance)

- `geneticAlgorithm_fork.py` — from the `xWuWux/geneticAlgorithm` fork's
  `master` branch (identical between the GitHub clone and the locally
  provided `geneticAlgorithm-master.zip`).
- `Genetic_algorithm_for_DNN_fork_original.ipynb` — the same fork's notebook.
  Its last line is a real syntax error
  (`final_text(pop_bag, text_summary))`) and cannot run as-is.
- `Genetic_algorithm_for_DNN_wojtek_copy.ipynb` — a separately patched local
  copy: fixes the syntax error, adds file logging and a `STEPS` variable.

These are not used by anything under `src/` or `tests/` — they're kept only
so the bug fixes documented in the top-level `README.md` can be traced back
to the exact original code they were found in.
